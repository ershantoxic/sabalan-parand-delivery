pluginManagement { def flutterSdkPath = { def p = new Properties(); file('local.properties').withInputStream { p.load(it) }; def sdk = p.getProperty('flutter.sdk'); assert sdk != null; return sdk }(); includeBuild("$flutterSdkPath/packages/flutter_tools/gradle"); repositories { google(); mavenCentral(); gradlePluginPortal() } }
plugins { id 'dev.flutter.flutter-plugin-loader' version '1.0.0'; id 'com.android.application' version '8.7.3' apply false; id 'org.jetbrains.kotlin.android' version '2.0.21' apply false }
dependencyResolutionManagement { repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS); repositories { google(); mavenCentral() } }
rootProject.name = 'delivery_visitor'
include ':app'
